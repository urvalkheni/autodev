# Task

Add `autodev watch` scaffold that monitors `.autodevs/todo/` for new files.

---

## Objective

Enable the voice-to-code supply chain by detecting DevMentor-committed todo files locally.

---

## Requirements

- Add fsnotify-based watcher in `packages/cli/cmd/`
- On new `todo_*.md`, print task id and path
- Call `InitDirs` to ensure Ralph Loop folders exist
- Document usage in CLI help text
- Do not implement full `run` executor yet — detection only

---

## Constraints

- Follow existing Cobra command patterns in `packages/cli/cmd/`
- Go only — no new runtime dependencies beyond fsnotify if already used
- Do not modify unrelated commands
- Write unit tests for path filtering logic

---

## Acceptance Criteria

- `autodev watch` starts without error in repo root
- New file in `.autodevs/todo/` triggers stdout notification
- `go test ./...` passes
- No changes to prompt capture engine behavior

---

## Priority

Medium

---

## Repository Context

AutoDevs v0.3.2 has prompt capture and DevMentor sync. Watch is the first step toward v0.4 autonomous pipeline.

---

Generated By:
DevMentor

Timestamp:
2026-06-10T00:00:00Z
